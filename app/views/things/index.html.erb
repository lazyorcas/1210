<% if @things.empty? %>
  <%= render partial: "nothing" %>
<% else %>
  <main class="w-full grow flex snap-mandatory snap-x overflow-x-scroll gap-4">
    <%= render partial: "thing", collection: @things %>
  </main>
<% end %>
