<% if @things.empty? %>
  <%= render partial: "nothing" %>
<% else %>
  <main id="things" class="w-full grow flex snap-mandatory snap-x px-4 overflow-x-scroll gap-x-2">
    <%= render partial: "thing", collection: @things %>
  </main>
<% end %>
