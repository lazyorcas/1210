<% if @idea.polled? %>
  <%= turbo_stream.remove @idea %>
  <%= turbo_stream.update "modal", "" %>
<% elsif !@idea.status_changed? %>
  <%= turbo_stream.replace @idea %>
  <%= turbo_stream.update "modal", "" %>
<% end %>
