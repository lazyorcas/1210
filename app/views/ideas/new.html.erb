<%= render ModalComponent.new(title: "New idea") do |component| %>
  <% component.with_body do %>
    <div class="px-4 modal-pb">
      <%= render "form", idea: @idea %>
    </div>
  <% end %>
<% end %>
