<%= link_to idea_path(idea), class: class_names("card flex gap-x-4", { "bg-neutral-900 text-white": idea.upvoters.exists?(current_user.id) }), id: dom_id(idea), data: { turbo_frame: "modal" } do %>
  <div class="grow">
    <p class="text-sm"><%= idea.user.name %></p>
    <h2 class="font-semibold"><%= idea.title %></h2>
    <p class="text-xs text-neutral-500"><%= time_ago_in_words(idea.created_at, highest_measures: 1) %> ago</p>
  </div>
  <div class="shrink-0 flex flex-col items-end">
    <% if idea.upvoters.find_by(id: current_user.id) %>
      <div class="flex items-center gap-x-1">
        <span class="rounded-full bg-green-400 h-2 w-2"></span>
        <p class="text-sm">Upvoted</p>
      </div>
    <% end %>
    <div class="flex items-center gap-x-1 text-sm">
      <%= heroicon "hand-thumb-up", variant: "solid", options: { class: "h-4 w-4" } %>
      <%= idea.upvoters.length %>/<%= idea.invitees.length %>
    </div>
  </div>
<% end %>
