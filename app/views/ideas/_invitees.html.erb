<div class="flex flex-col gap-y-4">
  <section class="flex flex-col gap-y-1">
    <header class="flex gap-x-2 items-center text-neutral-500">
      <%= heroicon "heart", options: { class: "h-4 w-4" } %>
      <h2 class="text-sm">Interested</h2>
    </header>
    <% if @idea.voters.empty? %>
      <p>None</p>
    <% else %>
      <%= render partial: "users/avatars", locals: { users: ([@idea.organizer] + @idea.voters).sort_by(&:name), show_name: true } %>
    <% end %>
  </section>
  <% if @idea.pending_invitees.present? %>
    <section class="flex flex-col gap-y-1">
      <header class="flex gap-x-2 items-center text-neutral-500">
        <%= heroicon "question-mark-circle", options: { class: "h-4 w-4" } %>
        <h2 class="text-sm">Pending / Not interested</h2>
      </header>
      <%= render partial: "users/avatars", locals: { users: @idea.pending_invitees.sort_by(&:name), show_name: true } %>
    </section>
  <% end %>
</div>
