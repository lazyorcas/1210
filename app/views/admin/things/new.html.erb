<%= render ModalComponent.new(title: "New thing") do |component| %>
  <% component.with_body do %>
    <div class="px-4 modal-pb">
      <%= render "form", thing: @thing %>
    </div>
  <% end %>
<% end %>
