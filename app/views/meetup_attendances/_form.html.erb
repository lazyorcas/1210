<%= form_with model: meetup_attendance do |form| %>
  <%= form.hidden_field :meetup_id %>
  <%= form.button :submit, class: "icon-btn btn--primary" do %>
    <%= heroicon "hand-thumb-up", options: { class: "h-6 w-6" } %>
  <% end %>
<% end %>
