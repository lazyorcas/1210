<%= render layout: "shared/modal", locals: { title: "Messages" } do %>
  <div class="mt-4 flex flex-col gap-y-8">
    <%= turbo_frame_tag "#{dom_id(@meetup)}_comments", class: "sm:w-[400px] flex flex-col gap-y-2" do %>
      <%= render @meetup_comments %>
    <% end %>
    <%= render partial: "form", locals: { meetup_comment: Meetup::Comment.new } %>
  </div>
<% end %>
