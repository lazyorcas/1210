<%= render ModalComponent.new(title: "Add memory") do |component| %>
  <% component.with_body do %>
    <div class="px-4 modal-pb">
      <%= render "form", past_meetup: @past_meetup %>
    </div>
  <% end %>
<% end %>
