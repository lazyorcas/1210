<!DOCTYPE html>
<html>
  <%= render "shared/head" %>
  <body>
    <header class="shrink-0 h-[68px] flex items-center gap-x-4 justify-between">
      <div class="flex items-center gap-x-1">
        <%= link_to ideas_path, class: "icon-btn icon-btn--small" do %>
          <%= heroicon "chevron-left", options: { class: "h-6 w-6" } %>
        <% end %>
        <h1 class="text-3xl font-black"><%= current_user.city %></h1>
      </div>
      <nav class="flex items-center gap-x-2">
        <%= link_to @only_current_user_interests ? things_path : things_path(interests: true), class: "icon-btn icon-btn--small" do %>
          <%= heroicon "bookmark", variant: current_page?(things_path(interests: true)) ? "solid" : "outline", options: { class: "h-6 w-6" } %>
        <% end %>
        <% if current_user.admin? %>
          <%= link_to all_things_path, class: "icon-btn icon-btn--small" do %>
            <%= heroicon "sparkles", options: { class: "h-6 w-6" } %>
          <% end %>
          <%= link_to new_thing_path, class: "icon-btn btn--primary", data: { turbo_frame: "modal" } do %>
            <%= heroicon "plus", options: { class: "h-6 w-6" } %>
          <% end %>
        <% end %>
      </nav>
    </header>
    <%= yield %>
    <%= turbo_frame_tag "modal" %>
  </body>
</html>
