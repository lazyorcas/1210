<div class="flex snap-mandatory snap-x overflow-x-scroll rounded-xl gap-4">
  <% photos.each do |photo| %>
    <%= image_tag photo_path(id: photo.id), class: "snap-always snap-center thumbnail rounded-xl", id: dom_id(photo) %>
  <% end %>
</div>
