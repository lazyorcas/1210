<main class="grow w-full flex flex-col gap-y-4 pt-4 pb-[84px] standalone:pb-[120px]">
  <%= turbo_frame_tag "memories", class: "flex flex-col gap-y-2" do %>
    <% if @memories.empty? %>
      <%= turbo_frame_tag "memories-header" do %>
        <%= render partial: "no_memories" %>
      <% end %>
    <% else %>
      <%= render @memories %>
    <% end %>
  <% end %>
</main>
