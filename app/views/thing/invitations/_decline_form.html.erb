<%= form_with model: @thing_invitation, url: thing_invitation_path(@thing, @thing_invitation), class: "form-flow" do |form| %>
  <%= form.hidden_field :is_accepted, value: false, required: true %>
  <%= form.button "Remove bookmark", class: "btn btn--secondary" %>
<% end %>
