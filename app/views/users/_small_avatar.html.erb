<% if user.avatar.attached? %>
  <%= image_tag photo_path(id: user.avatar.id), class: "w-8 avatar rounded-md object-cover" %>
<% else %>
  <div class="w-8 avatar rounded-md bg-neutral-900 text-white text-sm font-bold">
    <%= user.name.first %>
  </div>
<% end %>
